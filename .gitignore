.claude/
node_modules/
dist/
.DS_Store
Thumbs.db
*.log
.env

# Roh-Fotos vom Inhaber (Original-Uploads) - verarbeitete/optimierte
# Versionen liegen bereits in assets/images/, Rohdaten muessen nicht
# ins Repo (Groesse, teils mehrere MB pro Foto).
/ki 1.1/
/Bilder für unser Paradies/

# Lighthouse-Reports sind lokale Messergebnisse, kein Quellcode.
lighthouse-report*.json
lighthouse-report*.html

# Local Netlify folder
.netlify
# Wird von "netlify dev" automatisch angelegt (Edge-Functions-Bootstrap) und
# bei jedem Start neu erzeugt - gehoert nicht zur Website.
deno.lock

# Interne Unterlagen liegen bewusst AUSSERHALB des Repositorys, seit dieses
# oeffentlich ist:
#   C:\Users\Manuela\OneDrive\Desktop\zierfischparadies-intern\
#     docs\          Anleitungen, Betriebshandbuch, Launch-Checkliste, Rechts-Check
#     original-site\ Kopie der alten Website (fremde Gestaltungsarbeit)
# Die Eintraege hier verhindern, dass sie versehentlich zurueckwandern.
/docs/
/original-site/

# Ablage fuer andere Projekte und Rohmaterial, die zufaellig in diesem Ordner
# liegen, aber nicht zur Website gehoeren (z. B. "haus-1-store" aus einem
# anderen Vorhaben, sowie die unbearbeiteten Original-Fotos "ki 1.1").
# Ohne diesen Eintrag wuerden sie bei einem "git add -A" im Website-Repo landen.
/Projekte/
/haus-1-store/
